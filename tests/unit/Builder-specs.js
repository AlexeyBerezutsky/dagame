(function () {
    describe('Builder tests', function () {
        var builder;

        var gameMock = {};

        beforeEach(function () {
            builder = new Builder(gameMock);
        });

        it('lul', function () {
            console.log(builder);
        })
    })
})();